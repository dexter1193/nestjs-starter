version: "3"

services:
  postgres:
    container_name: postgres
    image: postgres
    ports:
      - 5432:5432
    environment:
      - POSTGRES_DB=your_choice
      - POSTGRES_PASSWORD=passw0rd
      - POSTGRES_HOST_AUTH_METHOD=trust